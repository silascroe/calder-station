{
  "name": "town",
  "version": "0.1.0",
  "private": true,
  "description": "A cost-aware persistent multi-agent town simulation.",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "scenario": "node scripts/run-scenario.mjs",
    "evaluate:model": "node scripts/run-model-evaluation.mjs",
    "evaluate:reflection": "node scripts/run-reflection-evaluation.mjs",
    "wait:evaluation": "node scripts/wait-model-evaluation.mjs",
    "validate:deploy": "node scripts/validate-deploy-config.mjs",
    "smoke:deploy": "node scripts/smoke-deployment.mjs",
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy"
  }
}
