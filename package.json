{
  "name": "town",
  "version": "0.1.0",
  "private": true,
  "description": "A cost-aware persistent multi-agent town simulation.",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "scenario": "node scripts/run-scenario.mjs",
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy"
  }
}
